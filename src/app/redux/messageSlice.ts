import { createSlice } from "@reduxjs/toolkit";

export const messageSlice = createSlice({
  name: "message",
  initialState: {
    //
  },
  reducers: {
    //
  },
});

export const {
  /** add actions */
} = messageSlice.actions;

export default messageSlice.reducer;

/**
 *  message reducer implementation
 */

///
